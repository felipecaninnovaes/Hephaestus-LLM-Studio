[project]
name = "hephaestus-trainer-difusao"
version = "0.1.0"
description = "Motor de treino Difusão LoRA (placeholder — treino real entra em slice posterior)"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = true
