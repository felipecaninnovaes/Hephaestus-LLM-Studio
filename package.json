{
  "name": "hephaestus-llm-studio",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "commitlint": "commitlint",
    "codegen:contracts": "npm run codegen:contracts --workspace=web"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "lefthook": "^2.1.12"
  }
}
