# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
/temp_redesign

# Rust / Node / Next / Python / env
target/
node_modules/
.next/
*.tsbuildinfo
__pycache__/
*.pyc
*.egg-info/
.venv/
.env
.env.*
!.env.example
dist/

# Orchestrator runtime outputs — fallback volume, never versioned
services/orchestrator/infra_outputs/
