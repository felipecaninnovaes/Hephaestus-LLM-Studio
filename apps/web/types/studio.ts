/**
 * Barrel canônico de compatibilidade reversa.
 * Re-exporta os tipos particionados por domínio em apps/web/types/*
 * (datasets, models, jobs, diffusion, yolo, telemetry, common).
 */
export * from "./index";
