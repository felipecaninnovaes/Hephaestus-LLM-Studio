[package]
name = "heph-contracts"
version = "0.1.0"
edition = "2021"
description = "Contratos e DTOs compartilhados do Hephaestus LLM Studio"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
